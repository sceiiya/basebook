{
  "name": "basebook-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for BaseBook remittance platform",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network baseSepolia",
    "verify": "hardhat verify --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  },
  "keywords": ["blockchain", "remittance", "base", "solidity"],
  "author": "MetaBase Team",
  "license": "MIT"
}
